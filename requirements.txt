# WebRTC driver for locked consumer Unitree robots (G1 + Go2) — the app's own protocol.
# It pulls in pyaudio (a C extension), so install pyaudio's prereqs FIRST — one of:
#   conda (recommended, no compiler/sudo):  conda install -y -c conda-forge pyaudio
#   apt (build from source):                sudo apt install -y build-essential portaudio19-dev
# ...then:  pip install unitree_webrtc_connect
unitree_webrtc_connect

# Optional: load a local .env automatically.
python-dotenv

# Phase 3 (unified REST/WS API) — uncomment when building server/app.py
# fastapi
# uvicorn[standard]
